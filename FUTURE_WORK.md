# Future Work

Outstanding, deferred, and explicitly-out-of-scope items, harvested from the plan/spec documents that drove Orca's evolution (now retired — the durable narrative lives in `ORCA.md` §14). This is a backlog of *what was deliberately left undone with enough context to resume*, not a roadmap commitment. **Completed items have been removed and their durable system facts folded into `ORCA.md` (§4 + §14);** this file now tracks only outstanding work and the ⚪ non-goals. File references are best-effort pointers and should be re-verified against current code before acting.

**Status hints:** 🔴 blocked on external info · 🟡 deferred by decision · ⚪ intentional non-change (do **not** "fix" without re-deciding — all ⚪ items live in [Appendix A](#appendix-a--non-goals--decided-against)).

## How this is sequenced

Work runs **from the substrate up**: lock the harness spine so it can't structurally drift, then activate each harness axis in turn, then catch the surfaces up, then pull the autonomy levers. Each phase has an **exit criterion** — the signal that the substrate is solid enough to start the next phase. You can walk the phases top-to-bottom; within a phase, items are ordered so the foundational ones come first.

| Phase | Theme | Exit criterion |
|---|---|---|
| **0** ✅ | Lock the spine | Substrate is runtime-enumerable; the engine is modular along its natural seams; no facet is hand-synced. **— COMPLETE (2026-06-26).** |
| **1** | Close the Governed axis | No ungated worker path exists; supervision settings behave correctly per-goal. **Antigravity gate is the one open item.** |
| **2** ✅ | Activate the Stateful axis | Belief-divergence is live end-to-end; the conflict WARN/auto path is reachable. **— COMPLETE (2026-06-30).** |
| **3** | Complete Executable & Inspectable | Full sensor ladder; OTEL verified on a live run; replay is correct. **Codex OTEL half is the one open clause.** |
| **4** | Surfaces & features on the harness | The conversational/adapter/template/workspace surfaces are caught up to the substrate. |
| **5** ✅ | Composition, learning & autonomy | The delegate seam and learning loop exist; the gate can drive itself in L5 (the L4→L5 crossing). **5.1 ✅ + 5.2 ✅ + 5.3 ✅ + 5.4 ✅ + 5.5 ✅ landed — Phase 5 complete (2026-07-04).** |

> **Guiding concern (applies to every phase).** Each architectural decision in Orca is individually defensible, but the sum — four harness axes × four facets × four boundaries × three adapters × graph-routed templates × recovery state machines × a versioned ledger, much of it converging in one daemon and one engine file — is a large surface area for a small team. The risk is good calls outpacing the capacity to keep them all correct (the ungated antigravity path in Phase 1 is the first visible instance). **Bias toward consolidation over new axes until the spine is runtime-enumerable and the engine is modular** — which is exactly what Phase 0 buys.

---

# Phase 0 — Lock the spine — ✅ COMPLETE (2026-06-26)

Done — the locked-spine shape lives in **`ORCA.md` §4 + §14**: self-registering facet/boundary/sensor registries (`defineFacet`/`defineBoundary`/`defineSensor`) with boot conformance guards, the `GET /v1/harness/registry` + `/v1/harness/hook-contracts` introspection routes, the `OrchestratorService` → `DispatchEngine` decomposition (with `ProviderRecoveryController` over a `RunnerPort` seam), and the per-adapter `hookContract()` boot guard that hard-fails startup on hook drift. The one settled non-change from this phase — the advance/route engine kept whole — is in [Appendix A](#appendix-a--non-goals--decided-against).

---

# Phase 1 — Close the Governed axis

The harness Governed axis is complete *except* for the antigravity gate below — a genuine safety hole. (The per-step/per-workflow **budget hard-stop** landed 2026-06-30 — durable shape in `ORCA.md` §14, Governed bullet.)

- 🟡 **Antigravity worker permission gate (the one open coverage gap).** `agy` workers spawn **ungated / fail-open** — no risk classification, no `tool_gate` transition, no safety floor; claude-code and codex *are* gated. `workerHookConfig` is a `{files:[], spawnArgs:[]}` stub in `apps/daemon/src/orchestrator-llm/providers/antigravity.ts`. **The 4 unknowns are now RESOLVED** (2026-06-26 spike: official docs at https://antigravity.google/docs/hooks + a live `agy` 1.0.13 smoke). The earlier "mirror Codex's hook file" premise was *partly wrong* — agy's hook + decision schema differ from Claude/Codex, and there is a genuine **worker-isolation** snag (below).
  - **Verified hook spec.** File `.agents/hooks.json` in the workspace (or global `~/.gemini/config/`). Events: `PreToolUse`/`PostToolUse` (matcher = tool name, e.g. `run_command`, `browser_.*`, `*`), `PreInvocation`/`PostInvocation`/`Stop` (no matcher, flat handler list — this flat form is exactly what the orchestrator's `Stop` hook already writes, which is why orchestrator Stop is genuinely fine). **PreToolUse format:** `{"<name>":{"PreToolUse":[{"matcher":"*","hooks":[{"type":"command","command":"…","timeout":30}]}]}}` (handlers nest under `hooks:[]` inside each matcher entry — NOT the flat Stop form). Optional top-level `"enabled":false` disables a named hook.
  - **PreToolUse stdin (camelCase):** `{toolCall:{name,args}, stepIdx, conversationId, workspacePaths[], transcriptPath, artifactDirectoryPath}`. **No `tool_use_id`** — synthesize a correlation id (like the Codex relay) from `conversationId`+`stepIdx`+sha1(`toolCall`).
  - **PreToolUse stdout:** `{decision:"allow"|"deny"|"ask"|"force_ask", reason?, permissionOverrides?:string[]}`. `deny` = hard-block immediately; `ask` = prompt (respects Always-Allow); `force_ask` = always prompt. (Field is `reason`, not the binary's internal `deny_reason`.)
  - **Hooks are interactive-only** (do not fire in `agy -p` print mode) — confirmed live. The worker already detects idle via pane capture, so the gate needs only the `PreToolUse` hook (no worker `Stop` hook required).
  - **Design (resolved part).** workerHookConfig emits `.agents/hooks.json` with a `PreToolUse` `matcher:"*"` hook + an **output-translating `.cjs` relay** (mirrors the existing `orca-stop-hook.cjs`): read agy's `toolCall.{name,args}` from stdin, synthesize a tool_use_id, POST `{tool_name,tool_input,tool_use_id}` to `/v1/agent-hooks/permission?sessionId=…` (reusing the *entire* existing gate — `classify.ts`→`decideGate` safety floor→`tool_gate` transition→operator await), then **translate** the daemon's `{hookSpecificOutput:{decision:{behavior:"allow"|"deny"}}}` into agy's `{decision:"allow"}` / `{decision:"deny",reason}`. Keep `permissionRule`/`writePermissionRule` no-ops (Phase 4). Flip the `worker-hook-config.test.ts` empty-stub assertion. Keep the `hookContract` worker entry `verified:false` until the live deny-blocking smoke passes in the **real daemon flow**.
  - **OPEN DESIGN DECISION — worker hook isolation (the only blocker left).** Claude uses `--settings <path>` and Codex uses `CODEX_HOME` to point the worker at an isolated `configDir`; **agy has NO per-invocation config/customization-dir override** (live-falsified 2026-06-26 against agy 1.0.13): `ANTIGRAVITY_EXECUTABLE_DATA_DIR` is **sidecar-data only** (not config/auth/hooks), `--user-data-dir` is **not a real flag**, and `GEMINI_DIR`/`GEMINI_HOME` do **not** relocate (every run falls back to `appDataDir=~/.gemini/antigravity-cli`; customization at `~/.gemini/config/` + workspace `.agents/`). So the CODEX_HOME-style relocation is **infeasible today** — agy reads `.agents/hooks.json` from the *workspace cwd*, which the orchestrator shadow session + the (sequential, ≤1-at-a-time) worker share. Remaining candidate resolutions: (b) one git **worktree per worker** so each gets its own `.agents/` (clean, but no worker-worktree infra exists today — larger scope; also reusable for Phase 5.1 isolated state); (c) **workspace-shared `.agents/hooks.json`** carrying both the orchestrator Stop hook and a worker `PreToolUse` hook, with request-time session correlation — since steps are sequential, the relay can embed `?goalId=…` (like the existing Stop relay) and the daemon resolves the active worker session for that goal, OR map agy's `conversationId` (present in PreToolUse stdin) → Orca sessionId. **Open subtlety for (c):** the orchestrator shadow session is *also* an agy process in the same workspace, so a workspace `PreToolUse` hook would fire for orchestrator tool calls too — confirm whether the orchestrator should be gated or the hook must scope to worker sessions only. Decide (b) vs (c) before implementing — it determines the workerHookConfig shape.
  - **Closing verification:** spawn a real antigravity worker via the live daemon (`daemon-terminal` tmux), trigger a tool, confirm `{decision:"deny"}` actually blocks and the payload maps correctly; then flip the contract to `verified:true`.

> **Exit criterion:** no worker provider spawns ungated; the global supervision flip and new-goal default both respect per-goal `operating_mode`. **(Antigravity is the one path still ungated.)**

---

# Phase 2 — Activate the Stateful axis — ✅ COMPLETE (2026-06-30)

Done — the Stateful axis is live and **live-confirmed end-to-end**; the durable shape lives in **`ORCA.md` §14** (the Stateful bullet): belief-divergence fires from a launch-vs-complete workspace version snapshot recorded on **every** launch path, the conflict policy derives from `operating_mode`, `mark_done` carries cumulative **write-set and cost** roll-ups (2.7), and a **fabrication-rollback** check rejects corrections that invent nonexistent file claims while recording verified claims as scoped evidence-bundle assumptions (2.8). The exit-criterion live smoke passed (real goal → mutate workspace mid-step → `belief_divergence` on `/harness-metrics`) — and caught + fixed a baseline-recording gap in `spawnStepAgent` along the way.

---

# Phase 3 — Complete the Executable & Inspectable axes

Sensor ladder + observability. The Executable sensor work folds onto the `defSensor` registry from Phase 0. The full ladder runs and replay is genesis-first/keyset-paged (see `ORCA.md` §14); these are the remaining axis capabilities.

## Executable axis

- 🟡 **Sensor capabilities beyond detection** (each its own seam): **oracle-adequacy enforcement** (`EvidenceFacet.oracleAdequacy` is computed at `runner.ts:74` but nothing vetoes on it yet), **artifact offload** of full sensor output (`artifactRef` is schema-only, always null — the eventual home for unbounded logs), and **per-workspace declarative sensor config** with route-by-type feedback.
- 🟡 **Per-ecosystem sensor resolvers** (Makefile / `cargo` / `pytest`, …). Auto-detect today is only `package.json` scripts. *(Note: `detect.ts` uses `npm run` while Orca workspaces are pnpm — works, a curiosity not a bug; see Appendix A.)* **Deferred-by-decision (2026-06-27):** no current workspace needs a non-npm resolver, so left additive per CLAUDE.md §2 — revive when a real consumer (a cargo/pytest workspace) appears.

## Inspectable axis

- 🟡 **OTEL Codex half.** Claude is live-confirmed end-to-end (`ORCA.md` §14; the live DB carries real non-null `cost.usd` rows); **Codex's struct-form `[otel.exporter."otlp-http"]` branch remains unexercised** — the one real gap left in the OTEL smoke (live DB has **zero** Codex cost rows). Note: `provider_id` is always null (distinguish providers by `model` prefix); Codex `mcp-server` spawn has no OTEL (interactive/`exec` only); antigravity has no usage channel → always `cost=null`. Needs a live Codex run to close the two-provider exit clause — and a **real product path** that selects Codex (all built-in `agentPreference`s list claude-code first, so Codex only runs when a goal's orchestrator provider is Codex or claude-code is unavailable; do **not** force it with a one-off codex-only template).
- 🟡 **Codex telemetry gaps.** `durationMs`/latency left null (no duration in `codex.sse_event`; `turn_ttft`/`api_request` shapes uncaptured); Codex cost estimate doesn't price cache tokens. Plus deferred per-tool durations/errors, ttft, edit accept/reject, LOC/commit/PR counts. **Deferred-by-decision (2026-06-27):** the tractable-deterministic parts (parsing `turn_ttft`/`api_request` shapes) and the live-data parts (real Codex cache-token pricing, latency) are best landed *together against the OTEL smoke* rather than guessing shapes blind — revisit alongside the live smoke.
- 🟡 **Price map as a config source.** Static in-code table today; a config source is additive/deferred unless it churns.
- 🟡 **`prompt_ref` / `raw_output_ref` artifact offload.** Confirm where offloaded artifacts live (handles, not inline) before populating.
- 🟡 **Dropped `step_complete` transition emits no metric** (deferred to the TelemetryFacet phase).

> **Exit criterion:** the full sensor ladder runs, OTEL cost is confirmed non-null on a live **two-provider** run, and replay keeps the oldest transitions. Sensor ladder ✅ and genesis-first replay ✅ are met; the **Codex half of the two-provider OTEL run** is the open clause (Claude is live-confirmed).

---

# Phase 4 — Surfaces & features on the harness

With the substrate locked and the four axes live, catch the surfaces up. These are largely independent of one another — order within the phase by what you're touching.

## Adapters & worker permission modes

- 🟡 **Antigravity native allow-list writer (Phase 4 of permission modes).** The Phase-1 item wires the *gate* (safety floor); this is the separate follow-on: `PreToolUse`/`request-review` shaping + native allow-list persistence so `writePermissionRule` stops being a no-op.
- 🟡 **Codex hook-trust persistence** as an alternative to `--dangerously-bypass-hook-trust` ("not explored"); the assumed 600s hook-timeout default remains unverified (only the per-hook `timeout` key was live-verified). *(Verified offline-analysis only: `--dangerously-bypass-hook-trust` is fully implemented on both Codex surfaces (`codex.ts:35,82`) and IS the current solution; no hook-trust-persistence mechanism exists or is reachable offline (`supportsPermissionPersistence=false`). The Codex Stop hook sets no explicit `timeout` (`codex.ts:237`), so the Codex CLI's own default governs — the 600s figure is a Codex-CLI fact not present in this repo and **only confirmable via a live Codex run / Codex docs**. Both halves stay deferred-to-live.)*

## Workflow step results & supervised completion

- 🟡 **Revision-signal consumer.** `step_revision_signals` is populated but nothing reads it. *Canonically owned by the Phase 5 learning loop (5.2) — listed here only as the data-capture pointer.*
- 🟡 **Telemetry counters** (`total_turns` / `tool_calls` on `WorkflowStepResult.performance`) stay optional until a reliable system source exists. *(Deferred-by-decision — fields stay optional/unpopulated; no reliable per-step turn/tool source exists (only dirty over-counting approximations from `activities`/`activity_steps`); revisit when the OTLP receiver provides per-step attribution.)*
- 🟡 **Scoring fill-rate eval gate** — no prompt-eval harness; fill-rate observed in practice post-ship. *(Deferred to a live-session effort — a ~sub-project blocked on a recorded-session fixture corpus + live model calls AND an unresolved design question (the `0.0` prompt example makes a real low score indistinguishable from a copy-paste, so "filled" can't be defined yet). Broker pass not reopened.)*
- 🟡 **Step terminal-event payload carrying full `stepResult`** — left conditional on the workflow event payload budget; events may carry identifiers only with consumers fetching the step run. *(Deferred-by-decision — terminal events carry identifiers only by design (4 KiB cap + content-free `FORBIDDEN_KEYS`); no consumer needs the full `stepResult` (desktop refetches on event type; projection/replay read the DB). Identifiers-only is the correct, exit-criterion-aligned choice.)*

## OrcaChat conversational surface

- 🟡 **Codex (and other adapters) support for the activity thread** — v1 is Claude-only. *(Offline half ✅ — provider-neutral contract pinned by a regression test (`provider-neutral.test.ts`): a no-transcript session yields a coherent thread with zero reasoning notes (silent no-op), confirming the `ActivitySignal`/`ActivityUpdater` contract is provider-agnostic. **Live Codex reasoning-path validation remains 🔴 PARKED** — needs a live Codex run on current daemon code. Reasoning-note extraction stays Claude-Code-transcript-specific.)*
- 🟡 **Optional `(Recommended)` badge detection** at render time (best-effort heuristic on Claude's label convention). *(Deferred-by-decision at sign-off — lowest-value, convention-sniffing render heuristic; not built.)*

## Graph routing, provider recovery, ledger & daemon addressing

- 🟡 **Daemon addressing — remote daemon.** Only the discovery-file/resolver seam exists; the remote endpoint + token refresh plug in later with no worker changes. *(Verified deferred-until-the-plane-split. The discovery seam IS generic — `DiscoveryRecord.url` is a bare string and `hooks-resolver/resolver.ts` uses it with no localhost assertion, so the "no worker changes" claim holds for the hook-delivery path; `RunnerPort` (`runner-port.ts`) is the named in-process precursor to FUTURE_ARCHITECTURE's network Runner Protocol. **But the daemon is a fused control+execution-plane process today** — remoting it now would either embed the execution plane remotely (wrong per FUTURE_ARCHITECTURE §2: PTYs/shadow-sessions/subscription/source must stay where the code lives) or serve only a niche power-user case; token-refresh/TLS/desktop-re-entrancy are exactly the parts that would bake in the wrong shape. Build the substantive endpoint **after** the plane split (Migration step 1). The one cheap, safe sliver — an `ORCA_ADVERTISE_URL` override to advertise a non-localhost URL in `daemon.json` — was scoped but **not built** (deferred with the rest to keep the seam coherent).)*
- 🟡 **Client-triggered re-adopt/respawn when daemon health is lost mid-use** — described (`lib.rs`) but has no dedicated automated test (manual smoke only).
- 🟡 **Prod-SEA packaging / Node availability** for the cross-platform resolver script; Windows file-permission handling is best-effort (chmod skipped). *(Verified. The main hook resolver is already SEA-safe — `hookResolverCommand` uses `process.execPath`/`ORCA_SIDECAR_BIN` (`config.ts:112-115`), not a literal `node`. Two **offline-visible** hardcoded-`node` sites remain — the antigravity `.cjs` Stop-hook command (`antigravity.ts:114`) and the Codex worker PermissionRequest relay (`codex.ts:229`) — but **both sit in parked flows** (antigravity is the ungated/parked provider; the Codex relay is the parked live-permission path), and full SEA behavior is **unverifiable without an actual prod-SEA build**. The "Windows chmod" concern is effectively a **non-issue**: resolver/hook files are invoked via `node <path>`, so no execute bit is set on any platform; the only `chmod` (`daemon.json`, `discovery-file.ts:21`) is already Windows-guarded. Deferred until a SEA build is the packaging target.)*

## Templates, onboarding & splitter

- 🟡 **Phase-2 platform-managed ledger for templates** (the `<orca:step-complete>` envelope, versioning, canonical-ID allocation, orchestrator review). *(Verified the engine machinery is **already complete** at the run level — every step commits a (currently empty) ledger version through `completeStepWithLedger`→`reviewAndNormalizeLedgerUpdates`→`commitLedgerVersion`. What's missing is purely agent-side: templates never **emit** `ledger_updates`. **Deferred to a live-daemon session** — the payoff (templates producing real ledger records) is unverifiable offline. The broker correction pass stays descoped (deterministic normalizer suffices). **Live-confirmed 2026-06-28:** a full adaptive-delivery run (triage→…→execution→validate_build→done) committed 16 ledger versions and **0 ledger records** — empirical proof templates emit no `ledger_updates`, so the desktop ledger panel correctly shows its empty-state until this authoring lands.)*
- *(The Fan-out / fan-in primitive that used to live here is the same delegate seam as Phase 5.1 — owned there.)*

> **Exit criterion:** the conversational, adapter, template, and workspace surfaces no longer lag the substrate — each consumes the harness facets/boundaries directly rather than via synthetic/transient shims.

---

# Phase 5 — Composition, learning & autonomy

Design proposals harvested from an engine design review, **not commitments** — re-verify anchors and re-decide scope before acting. The throughline is two levers Orca has the prerequisites for but hasn't pulled — **composition via a delegate seam** and **learning from execution evidence** — plus the **L4→L5 autonomy crossing**. All deliberately scoped to stay within Orca's worldview (TypeScript, deterministic engine, the engine owns the lifecycle).

**Status at a glance:** 5.1 ✅ landed (Phase 5E) · 5.2 ✅ landed (2026-07-04; propose/promote + counterfactual judge; **replay re-run remains the sole deferred path**) · 5.3 ✅ landed (2026-07-03) · 5.4 ✅ landed (2026-07-04) · 5.5 ✅ landed (2026-07-04). **Phase 5 complete.**

- ✅ **5.1 Sub-workflow composition — a `workflow` operator / delegate seam (highest-payoff item).** **LANDED (Phase 5E) — the single-child `delegate` seam.** A fourth graph node type, `delegate`, spawns a child `WorkflowRun` of another version-pinned template on the same goal; the parent parks in a new `delegating` status while the child runs as the active leaf, the parent↔child composition row makes the delegation stack durable across restart (depth-guarded, cycle-rejecting), and the child runs with an **isolated state space** — parent values cross in only through the delegate node's typed `reads` map and the child's terminal output crosses back only through `writes`, validated against the child's `inputs` + terminal `outputSchema`. Spawn/join is control-plane `DispatchEngine` logic; child steps ride the existing `RunnerPort` (runner-agnostic); the join is verdict-gated and wired across all four harness axes (composition-spanning budget, `operating_mode`-derived conflict/launch policy + auditable `GoalDecision`, belief-divergence at join, `delegate_spawn`/`delegate_join` transitions). One composed built-in dogfoods it end-to-end: `orca/scoped-delivery` → `orca/scope-brief`. **Deferred (documented, not hidden):** fan-out (the same seam over N children under one parent — the shape is fan-out-ready), outcome-port branching off the delegate result, semantic-merge of child writes (today: typed map-back, no merge), workspace file-rollback on child failure, per-child runner placement, and MAS-level failure attribution across the delegation stack. Three implementation-discovered deferrals: (1) **I3 belief-divergence granularity** — the join compares the shared `VersionProbe`'s `branch`+`dirty` signal (consistent with the system's existing belief-divergence), so a same-branch *clean* commit advance under the child is not detected; the probe is injectable, so a commit-hash-aware probe can drop in later. (2) **Desktop nested-run graph view** — the delegate node visual + the delegation breadcrumb (parent↔child navigation) landed, but rendering the child run's *graph* inside the list-based `WorkflowRunPanel` is a follow-on (the `nodeStatuses`/`highlightNodeId` hooks are in place). (3) **Visual editor for template `inputs`** — the daemon + contract now persist `inputs` (migration `0051`), but a user can only author them via the API/catalog, not the workflow editor UI. *(Original framing:)* Templates were flat graphs — graph nodes `step|gate|splitter` only, no node referencing another template id; the prerequisites (immutable `template_snapshot_json`, versioned templates, the node cursor, branch-source-agnostic terminal-reachability validation) were already in place.
- ✅ **5.2 Close the learning loop from captured evidence.** **LANDED (2026-07-04).** The propose/promote half has landed (2026-06-30, Phase 5B): a per-template reflective optimizer proposes (never silently applies) targeted edits to step `instructions`, opt-in and gated — deterministic diagnosis (rules R1–R4, sample-gated, instruction-addressable failure codes only, top-3 cap) + one gated LLM proposal call per qualifying step, each carrying a full change contract (component, targeted failure mode, predicted improvement, invariants, falsifier `version_comparison`, rollback plan); a route-gated privileged write applies the human-confirmed edit (forward version only — append-only, no history rewrites). Per-template only — not cross-goal (non-negotiable). The **counterfactual-judge evaluate stage has landed:** a pre-promotion, human-triggered isolated adversarial shadow turn over persisted past outputs, bucketed by independent `RefuteFacet`/`EvidenceFacet` ground truth, returning a calibrated write-once `CounterfactualJudgment` that informs (never gates) promotion; it is imagined execution over persisted outputs (control-plane-pure via the `ShadowAsk` seam). **Deferred (documented, not hidden):** **replay re-run** (re-run affected steps against past goals via the execution plane) — a separate future sub-project that needs the execution-plane seam. This is the sole remaining pre-promotion-evaluation path. **Extended (2026-07-06, SP2):** a second revision target landed — R4 ("false confidence") deterministically routes to a tightened step `outputSchema` proposal (the deterministic completion validator), gated by a code-enforced mutation whitelist (additive/strictening ops only — never delete/rename/retype/alter-enum/weaken — protecting the splitter `branchKey` and delegate `writes` composition contracts) and a schema-specific invalid-output canary (`versionInvalidOutputRateDelta`, observational, same `VERSION_MIN` gating); the Self-Improvement panel reached mock-parity fidelity (diff modal, honest judge/falsifier display, no invented confidence). **Extended (2026-07-06, SP3) — LANDED.** Every proposal-lifecycle transition (created/judged/applied/dismissed/rolled_back/superseded/baseline_restored, plus a per-analyze `analyzed` event) now writes append-only to `learning_events` (migration `0056`) in the same transaction as the state change, template-version-stamped, with the `rolled_back` event freezing the falsifier's outcome snapshot before version churn — the learning loop's append-only spine. A display-only calibration readout (`computeCalibration`) compares each tier's designed `TIER_CONFIDENCE` prior against measured survival among independently-concluded claims, in three honest states (measured/insufficient/unmeasurable), surfacing a per-step insight only past an n≥10, >0.2-divergence threshold — the score's coefficients stay untouched. The Self-Improvement rail's activity log became a plain-language learning-log timeline, with pre-SP3 proposals rendered from a synthesized, visually-marked line (no real events exist for them). Calibration is observational and windowed, not an all-time decayed rate. **Remaining future work:** owner-scoping of proposals/events (the tenancy seam, tracked with the rest of Phase 5's owner-scoping items), per-template-*version* calibration (today's readout pools across versions within the window), coefficient governance (recalibrating `TIER_CONFIDENCE` itself against measured survival, currently a human/code decision, not automatic), event retention/pruning policy for `learning_events` (append-only, unbounded today), and the other revision targets (topology, retry limits, gates, guardrail configs) behind the `component` seam.
- ✅ **5.3 Make the gate the loop's objective + wire the LLM gate-evaluator (the L4→L5 seam).** **LANDED (2026-07-03).** Orca is most of the way there: a rejecting gate already surfaces `{reason, issueRefs}` as `repairContext` injected into the re-run prompt (`latestRejectingGate`, `service.ts:1362,2583`; asserted in `service.gate-routing.test.ts`), and gate criteria are runtime-derived from goal + ledger + instructions. The gap: gates are resolved **only by a human** today (`service.ts:3795`; `parkForGateApproval` `:3800`, `decideGate` `:4131`) — yet the full `GateEvaluationRequest`/`GateEvaluationProposal` contract is already built and unwired (`packages/contracts/src/workflows/index.ts:784-812`). Derive the gate from the goal, branch on the verdict, route the **failing criterion** to the closing step (Orca's backward-edge routing already does this), and **terminate honestly** when a goal is unachievable rather than spinning. **Resume:** wire the dormant evaluator to drive gates in `operating_mode = automated` (L5) while keeping the human verdict for `human_review` (L4). This is the natural place the Governed axis crosses from supervised to autonomous. **Related (external-eval, 2026-06-28) — issue-list-driven correction.** The `{reason, issueRefs}` repair channel exists but human gate decisions always write `issueRefs: []` (`dispatch-engine.ts:1641-1651`) and the LLM `revise_step.feedback` is a single free-form string (`orchestrator-llm/prompts.ts:96`); only the *deterministic* revision paths emit structured violation lists today (schema errors `judgement.ts:45`; per-sensor failures `service.ts:1169-1182`). The paper's PEV loop routes validation as *bounded* "continue/revise/stop" feedback (p.28) — so have the gate/LLM paths emit an **enumerated issue list** ("fix only these; do not rewrite what's correct") routed to the failing step, so the N=3 revise cap (`revise-loop.ts:1`) isn't burned by over-correction. Stays inside the deterministic-core invariant (the engine routes the issues; the LLM only fills the list). Land alongside the gate-evaluator wiring here; 2.8's fabrication check is one such issue producer. **Resume note:** the automated gate evaluator is now wired into production via the `ShadowAsk` seam (`shadowSessions` in `server.ts`); the L4 human path is unchanged; the issue-list channel is lit on the automated-gate path (`issueRefs` flows on rejection); `revise_step.feedback` enumeration and human-gate-decision `issueRefs` enumeration stay deferred to 5.4.
- ✅ **5.4 Verify both halves — independent/adversarial check on self-reported scoring.** **LANDED (2026-07-04).** Step completion is hybrid-gated (deterministic schema validate → LLM judge), which is sound — but the `approve_step_complete` *scoring* block (outputCompleteness/correctness/instructionAdherence/downstreamReadiness/riskLevel, `orchestrator-llm/prompts.ts`) is **self-reported by the same orchestrator-LLM that approves the step**. A green deterministic skeleton wrapped around garbage LLM output is still a failure. It shipped as a risk/coverage-gated **refute** pass in `approve_step_complete`, after the deterministic evidence gate passes and before the completion commits: `shouldRefute` fires on high/critical `tool_gate` risk, a missing sensor oracle, or oracle coverage gaps (a well-verified low/medium-risk exec step skips it — not universal). The refute itself (`refuteStepCompletion`) is a single adversarial shadow-LLM turn, independent of the approver via a dedicated session key (`${goalId}::refute`), oracle-scoped so it only targets the unverified surface, and calibrated to a tri-state verdict (`upheld`/`refuted`/`uncertain`, plus an engine `unavailable` on call failure) — `uncertain`/`unavailable` escalate to a human rather than guessing. Every refute that runs is an inspectable `RefuteFacet` on the `step_complete` `HarnessTransition` (`refute_veto` failure code on veto) — the adversarial-refute plumbing 5.2's counterfactual judge was waiting on. **L5** routes `refuted` back to `reviseStep` (bounded issue list, N=3 cap intact), `uncertain`/`unavailable` to a human pause, `upheld` to commit — the deterministic core owns every branch. **L4** is strictly advisory: the verdict rides `pending_completion_json` and the confirmation card's lead (`confirmationLead` prepends the advisory when the verdict isn't `upheld`) plus a desktop chip + reason + issue list on the **live pending** confirmation card; after the human resolves, the structured chip is not rebuilt (only the prose advisory persists in the completed-step card's lead — an honest gap; persisting the structured verdict past resolution is a follow-up). The human stays authoritative and the refute never re-gates on resume. **Note (external-eval, 2026-06-28) — the "advisor" framing.** An external harness fires a critique *before* the reviewer sees the work (proceed | revise | escalate), respawning the writer early; Orca's `critique` is a full downstream *step* (`templates/catalog.ts:135`) and gates only react `approved | rejected` after completed output (`contracts/.../index.ts:816`) — no pre-approval draft advisor. This 5.4 refute pass **is** that advisor done right: a risk-gated refute *before* `approve_step_complete` commits is a pre-commit verify in the paper's PEV sense, composing with the deterministic gates rather than adding a new LLM-driven node type.
- ✅ **5.5 Reasoning-first ordering on structured orchestrator outputs.** **LANDED (2026-07-04).** Every LLM judgment node now emits a required, leading `reasoning` field before the structured verdict (chain-of-thought as schema structure). It is persisted as an inspectable reasoning-trajectory (additive blobs on verdict-type entities, plus migration `0054` for gate/split decisions), bounded by the cost spine (`REASONING_MAX = 2000`), with engine-null reasoning on unavailable verdicts. **Caveat: Split is future-facing** — `evaluate_split` is currently unwired in production, so the splitter's reasoning-first takes effect only when that transport wires.

- 🟡 **5.6 Node confidence model — evidence-based per-node scoring.** Design: `docs/superpowers/specs/2026-07-24-node-confidence-model-design.md`. Reframes every graph node's score as *calibrated confidence it did its job correctly, given evidence* (measured, not asserted priors; no per-type ceiling; first-class uncertainty; retrospective vindication attenuated back from hard anchors — executable tests + human sign-off). This is the concrete plan behind several 5.2 "remaining future work" items, notably **coefficient governance** (making calibration load-bearing on the score). Four-phase decomposition; **Phase 1 LANDED (2026-07-24)** on branch `node-confidence-model` — the scoring engine: `composedScore` returns an **unknown** state instead of the `self_report` 0.3 floor (unchecked steps read "needs evidence," not a fake ~30), and **worker-gate approvals now count as independent-review evidence** for the gated step, resolved read-time from graph topology + version-filtered to the latest shape (no migration). **Deferred from Phase 1 (the guaranteed-verifier invariant's *hard enforcement*):** `validateStepVerifiers` (in `apps/daemon/src/workflows/graph/validate-graph.ts`) is implemented, unit-tested, and Adaptive-catalog-verified, but ships **dormant / unwired** — a proactive audit found **6 of 7 built-in templates** (`bug-triage-fix`, `code-review`, `refactor`, `quality-coverage`, `scope-brief`, `scoped-delivery`) plus the **graph-null auto-materialization default** contain verifier-less steps (only `orca/adaptive-delivery` passes). Turning on hard enforcement (reject a verifier-less template on create/update — the wiring sat at `routes.ts:114/143`, reverted) requires first assigning each bare step a verifier (enforce-grounding, a `validation_rule`, a gate, an interview/handoff policy, or terminal) and deciding the materialization default's verifier — a catalog-wide remediation, its own scoped effort. **Phase 2 (split 2a → 2b):** **2a LANDED (2026-07-26)** — the downstream-vindication signal (`deriveVindication`): each step's final completion labeled vindicated/bounced/pending by its immediate downstream (gate/splitter/delegate/step/terminal→human `mark_done`), read-time, version-safe, surfaced observationally (`StepMetrics.vindication`), carrying `byNodeId` for attenuation. **2b LANDED (2026-07-26)** — load-bearing empirical-Bayes: per-source `Beta(α₀=prior·4, β₀)` calibration fed by refute (executable/grounding only — CRUX) + **prior-weighted** vindication labels (weighted by `vindicatorWeight` — worker gate 0.55 / shadow 0.4 / step 0.5 / splitter 0.3 / terminal 1.0), `sampleSize`=Σweights (anti-gaming: weak vindicators need proportionally more evidence); **`independent_review` now calibrates via vindication (non-circular)** and moves the score; governance-bounded (advisory, inspectable via `calibrationMix`, never gates). **2b follow-ups — RESOLVED (2026-07-27):** (1) summary/detail calibration divergence fixed — `getTemplateMetricsDetail` now computes the vindication-aware calibration ONCE and shares the same array with both the summary and the step scores (true single-source, no drift); (2) `independent_review` coverage denominator now scoped to labelable (vindication-keyed) completions so older-version completions don't dilute it; plus an edge fix (a non-empty `independent_review` bucket with zero labelable completions is `unmeasurable`, not `insufficient`). **Deferred within Phase 2 (by design):** *computed-confidence* attenuation (2b uses prior-weighted; upgrades once gates/splitters are scored), the human-goal-change ask-stability "pivot" case, and full graph propagation (lands with autonomy-gating). **Phase 3 LANDED (2026-07-27):** gates & splitters scored as nodes. `deriveGate/SplitterVindication` (`node-vindication.ts`) label each gate/splitter DECISION vindicated/false_accept/pending vs the terminal anchor (`mark_done`; hard-fail OR `goal_archived`/`session_archived` abandonment = false_accept), carrying `byNodeId` (propagation-ready edge). Gate `decisionConfidence` = per-gate Beta over false-accept (approval held up), prior worker 0.7/shadow 0.55, added to `GateMetrics` (existing health/overturn/groundedness kept). New `SplitterMetrics` + `detail.splitters` = branch-not-walked-back confidence, `retrospectiveOnly`, with **T4 attribution** (`deterministic`/`attributedToNodeId`=upstream step, since a deterministic `branchKey` splitter forwards an upstream decision). `betaMean`/`betaSampleSize` extracted (behavior-preserving). Version-safe; advisory/never-gates; statistical aggregate rate (state/sampleSize surfaced, no per-instance root-cause). **Deferred (still, by design):** computed-confidence attenuation (needs the topology-ordered propagation engine — lands with autonomy-gating; `byNodeId` edge graph now accrues for it); the staged `selectedEdgeTo`/`selectedBranch` remain unused (finer misroute detection). **Phase 4 (not started):** the drawer's system-owned, node-naming, no-jargon confidence-reason vocabulary. **Phase-4 display requirement (from P3 review):** render `decisionConfidence.value`/splitter `confidence.value` ONLY when `state === "measured"` — the value is present even at `insufficient` sample size (advisory), so a thin-sample number must not be shown as trustworthy. **Phase 1 decision (independent-review compounding):** Phase 1 **caps** independent-review at a single 0.55 (gate approval or refute-upheld, or both — does NOT compound, since correlated LLM adversarial reviews shouldn't be treated as independent before measurement); Phase 2 calibration can lift it if double-reviewed completions measurably survive better.

> **Exit criterion:** the delegate seam runs a child workflow with an isolated state space; the learning loop proposes (never silently applies) template edits; and a gate can drive itself to a verdict in L5 while L4 keeps the human.

---

# Appendix A — Non-goals & decided-against

⚪ Intentional non-changes. Do **not** "fix" these without re-deciding — grouped by area.

**Governed / safety**
- **Real OS containment (`SpawnSandbox`).** The seam exists as a default no-op pass-through (`apps/daemon/src/adapters/sandbox.ts`); actual OS sandboxing (macOS `sandbox-exec` / Linux namespaces+seccomp / Windows) is explicitly out of scope. Agents run in the real repo with inherited credentials.
- **`classify.ts` `\bnc\b` over-match** (errs fail-safe / over-escalates); the **double-namespaced `operator:agent:claude-code` risk label** (revisit only with a real operator taxonomy — change producer + matcher together); the **inert `autonomyLevel` column** (SQLite column drops are messy; left in place).
- **Autonomous Evolution Agent / self-modifying harness (L5)** — explicit non-goal; only the read-side failure-attribution substrate (categorical codes + clustering) is in scope.
- **Cross-goal / global memory** — non-goal; experiential memory stays within per-goal templates (this also bounds the Phase 5.2 learning loop to per-template).
- **LLM-authors-and-runs-the-workflow-at-runtime** — the opposite of Orca's load-bearing rule that the engine owns the lifecycle and the LLM never gets tool-call freedom to advance steps.

**Engine structure**
- **Advance/route engine kept whole.** `commitAdvanceOrComplete` + gate/splitter routing (`parkForGateApproval`, `evaluateAndParkSplitter`, `routeGateDestination`, `decideGate`, `confirm*`) is the orchestrator's irreducible dispatch core — now the `DispatchEngine` (`ORCA.md` §4). It recurses into `requestNextDecision`/`spawnStepAgent` with a `routeGateDestination ⇄ evaluateAndParkSplitter` cycle; extracting it piecemeal needs ~7 injected callbacks (a pass-through, not a seam). FUTURE_ARCHITECTURE: "deterministic code owns lifecycle, routing, gates" (one core). Documented in a code-comment on `commitAdvanceOrComplete`; do **not** force-extract.

**Stateful axis**
- **LLM semantic conflict-judge.** Only the no-op `ConflictJudge` seam shipped (`noopConflictJudge` keeps every overlap as a real conflict). An LLM judge gated behind the deterministic overlap signal (reusing the `SessionMemoryExtractor` seam) is a documented later drop-in — no LLM call today.
- **Sibling-session summaries in launch read_set** deliberately omitted (`summaries: []`, no exported reader exists).
- **No pessimistic locking, no auto-merge, no cross-goal state** — optimistic detect-don't-prevent is the chosen model.

**Executable axis**
- **`detect.ts` uses `npm run`** while Orca workspaces are pnpm — works, noted as a curiosity, not a bug.

**Inspectable axis**
- Full event-sourcing of all Orca state; external OTEL collector / Prometheus-Grafana; per-subagent within-worker token attribution; global / cross-owner dashboards (per-template cross-run is now in-scope as the 5.2 substrate).

**Adapters & permission modes**
- **Codex per-command "remember" / `writePermissionRule`** — Codex uses a global `approval_policy`, no per-command rule; stays a no-op.
- **Persisting *deny* decisions** — only `allow` + `remember` writes a rule.
- **Orca-maintained permission allow-list ("Strategy A")** — abandoned; relies entirely on each CLI's native residual-permission event.
- **Direct Google API model provider** (non-agent orchestration) — explicit non-goal of the antigravity adapter; `orca/google` is agent-backed metadata only.
- **Pending approvals are in-memory only** — do not survive a daemon restart (accepted tradeoff).

**Workflow step results & autonomy**
- **Per-goal supervision override** and **finer-grained autonomy gradations** — autonomy stays the binary Supervised (L4) / Autonomous (L5) `operating_mode`; the dormant `goals.autonomy_level` integer is left inert.
- **Parallel/concurrent step execution** — multi-cursor traversal and join semantics out of scope (validation is written fan-out-ready; steps stay single-outgoing-edge).
- **DB migration of existing user templates** for the new terminal-reachability rule — validation applies only on next edit. No step-level `terminal` marker on the step contract (graph node `terminal` is the single source of truth).

**OrcaChat**
- **LLM polish of Orca-voiced worker-question text** — deterministic templating only for v1.
- **No retro-migration of worker questions** already pending at upgrade time — they keep the old activity shape. Orchestrator `ask_user` questions and permission approvals are unchanged by the free-text / persist-answered arcs.
- **`git diff`-based diff capture** and a **per-turn consolidated "Changes" card** — rejected in favor of hook-reconstructed per-edit diffs.

**Graph routing, provider recovery & daemon addressing**
- **Provider-recovery non-goals:** automatic provider switching in Auto-run, predicting quota availability when the provider doesn't report it, sharing native conversation state across providers, billing/quota-purchase flows.
- **Daemon-addressing out of scope:** retrofitting in-flight workers (they age out); auto-stop / idle daemon shutdown (explicit stop + OS logout only).

**Templates, onboarding & splitter**
- **`INITIATIVE_GRAPH` does not exist** — subsumed into `orca/adaptive-delivery`; the catalog reconcile test's `goals` INSERT has no schema drift (omitted columns carry defaults); `orca/feature-development` has zero code hits.
- **Roles / `roleLabels`** — out of scope across catalog and onboarding (display metadata only; no role model, no role chips).
- **Per-template enable/disable after install**, **cross-goal knowledge graph**, and **visit-limits / loop caps on routing** — explicit non-goals. The "Feature Development → Feature Implementation" rename lands only on fresh inserts (version-guarded upsert).

**Brainstorm**
- **No visible gate node for the open-questions check** (enforced as a completion invariant), **no per-item blocking-vs-note agent classification** (the step's `completionPolicy` decides), and **scoring math / quality dimensions unchanged**.

**Workspaces (out of scope v1)**
- Delete-workspace mutation; live-session indicators/counts on goal cards & workspace rows; associating an existing goal to a workspace from the Workspaces tab; workspace color/icon/slug; persisted live git state on the entity; a pre-submit liveness check for moved/deleted registered folders (the daemon's submit-time re-inspect is the only guard).

---

# Appendix B — Reference & hygiene

### ORCA.md hygiene

A few subtler ORCA.md claims were **not** independently verified and are left as-is pending a code check: the memory lifecycle tiers (`observed → extracted → promoted → canonical`), the "reconstruct a Goal by replaying its events" claim, and the "LLM-driven extraction" framing. Verify against current code before trusting or editing them.

### Known test flakes (acknowledged, not root-caused)

- `http-surface.test.ts` and `human-review.test.ts` (15s timeout) — time out under parallel load, pass in isolation.

### Typecheck gate blind spot (hygiene)

- `noUnusedLocals` is enabled for the daemon (`apps/daemon/tsconfig.json`) but does **not** flag unused module-level *exports* — `pnpm knip` covers that gap (it is how the orphaned `selector.ts` was eventually caught). `noUnusedParameters` is deliberately OFF (intentional unused-param hits).

### External-harness practice eval (2026-06-28)

A practice comparison against another harness flagged six items; all verified against code (file:line in the folded items elsewhere in this doc). Recorded here so the two that are **not** gaps aren't "rediscovered":

- **Already satisfied — deterministic pre-checks before the LLM judges.** Orca already gates schema + ledger validity *in code before any orchestrator-LLM call* (`workflows/orchestrator/judgement.ts:30-48`, proven by `judgement.test.ts:16-26` asserting `mediator.invoke` is **not** called on invalid output) and adds post-approval evidence/conflict **veto** gates (`service.ts:1057-1196`). This is exactly the paper's PEV ordering — *deterministic sensors + human-review gates*, the LLM invoked only where judgment is needed (the "deterministic core, selective AI" invariant). The LLM never owns the whole quality gate, so the external claim that this is the "biggest gap" does not hold for Orca.
- **Process, not code — per-step behavioral specs.** Each template step already carries its responsibility (`instructions`) and required output artifact (`outputSchema`) in `workflows/templates/catalog.ts`, enforced by `output-schema.ts`. Treat divergence-from-spec as a review signal rather than authoring a parallel `outlines/` dir.

The genuine gaps from this eval were folded into the phases: **Phase 1** budget hard-stop (permission-tier / cost-spine) ✅ 2026-06-30, **2.8** assumption-level claim verification (fabrication rollback, Stateful axis) ✅ 2026-06-30, **5.3** issue-list correction ✅ 2026-07-03, **5.4** advisor framing ✅ 2026-07-04.
